const array = [1, 'texto', 4.3, false, {propiedad: 'valor'}];

const amigos = ['Alejandro', 'Manuel', 'Cesar'];
console.log(amigos[1]);

// agregar informacion al array
const colors = [];
colors[0] = 'Red';
colors[1] = 'Green';
colors[3] = 'Blue';
console.log('El array tiene: ' + colors.length + ' colores')

colors.push('Azul');

// prueba 1
let = 0;
